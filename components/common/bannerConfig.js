// Configuration for the announcement banner
export const bannerConfig = {
    // Enable or disable the banner globally
    enabled: true,

    // Messages to display (will scroll continuously)
    messages: [
        "Use Code WELCOME10 & Get 10% Off on Orders Above ₹399! 🎉"
    ],

    // Styling options
    backgroundColor: "#FFC107", // Yellow/amber color
    textColor: "#ffffff",

    // Animation settings
    speed: 15, // seconds for one complete scroll (lower = faster)
    pauseOnHover: true, // Pause animation when user hovers
};
